Live product URL:
https://ai-native-docs-1.onrender.com

Sign in with a seeded account (password for all: demo1234):
- alex@ajaia.dev
- jordan@ajaia.dev
- sam@ajaia.dev

This is the Render static frontend. Set `VITE_API_URL` at build time to your Render API service (the Express app), then redeploy. Direct routes like `/dashboard` need the `_redirects` rewrite in `frontend/public/_redirects`.

Local:
- Frontend: http://localhost:5173
- Backend: http://localhost:4000
